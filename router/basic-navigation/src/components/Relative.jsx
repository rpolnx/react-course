import React, { Component } from 'react'

export default class Relative extends Component {
    render() {
        return (
            <div style={{background: "green", padding: "20px", textAlign: "center", color: "white", maxWidth: "600px"}}>
                Relative one
            </div>
        )
    }
}
